# ============================================================
# ВЛАДЕЛЕЦ: P3 (зависимости + стабильность деплоя)
# torch/torchvision зафиксированы под CUDA 12.1 (совместимо с драйвером 12.2 на L4).
# Ставить с --no-cache-dir (диск ~5 ГБ): pip install --no-cache-dir -r requirements.txt
# ============================================================

# torch cu121 (Linux/сервер). Индекс PyTorch берётся первым для torch/torchvision.
# NB: точный пин 2.3.1/0.18.1 пропал из индекса cu121 (остались только 2.6.0+) —
# ставим нижнюю границу вместо точного пина, чтобы pip не падал при недоступности
# старой версии. Если нужна конкретная версия — уточните и зафиксируйте заново.
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.3
torchvision>=0.18

# Core numerics / imaging
numpy>=1.24
scipy>=1.10
opencv-python-headless>=4.8      # headless: без GUI-libs, работает в Docker + Streamlit
scikit-image>=0.21
Pillow>=10.0

scikit-learn>=1.3               # GroupShuffleSplit, macro-F1, confusion matrix

# API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9         # загрузка файлов

# UI
streamlit>=1.33

# Отчёты / экспорт
reportlab>=4.0                  # PDF
pandas>=2.0                     # CSV / таблицы

tqdm>=4.66

# Опционально: SAM для границ (Apache-2.0), НЕ ставится по умолчанию: pip install segment-anything
# НЕ использовать ultralytics/YOLO (AGPL).
